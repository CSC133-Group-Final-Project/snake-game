package com.proj.snake.interfaces;

// This interface should be implemented by any class representing an entity that can be reset.
public interface IResettableEntity {
    // Resets the entity to its initial state.
    void reset();
}
